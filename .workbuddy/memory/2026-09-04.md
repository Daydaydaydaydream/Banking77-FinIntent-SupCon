# 2026-09-04

## 用 miniconda 建环境并跑通项目

**环境**（用户明确要求用 miniconda，不用 WorkBuddy venv）
- conda 25.1.1，`/opt/homebrew/bin/conda`，osx-arm64
- 新建环境 `/Users/jingzhefeng/.conda/envs/banking77`（Python 3.13.15）
- 按 `requirements.txt` 装齐，另补 `matplotlib`（analysis 模块需要，requirements 里漏了）
- MPS 可用，`torch.backends.mps.is_available() == True`

**两个坑（都值得记住）**
1. **沙箱打断 conda 的 codesign 步骤** → 环境里 28 个 Mach-O 二进制签名失效，
   macOS 启动即 SIGKILL（exit 137），表现像"python 装坏了"。
   修法：遍历环境重签 ad-hoc：`codesign -s - -f <file>`。
   脚本在 `/tmp/resign_conda_env.py`。
   注意：今后 `conda create` / `conda install` 建议直接关沙箱跑，避免再次踩坑。
2. **huggingface.co 和 hf-mirror.com 本机都不通**（代理 502 / 直连超时），
   只有 PyPI 系可达。记忆里"HF_ENDPOINT=hf-mirror.com"这条已失效。
   变通：从 **ModelScope** 拉模型，按 HF 缓存布局落盘到
   `~/.cache/huggingface/hub/models--<org>--<name>/{refs,snapshots}`，
   这样 `from_pretrained("bert-base-uncased")` 原样可用、不用改任何代码。
   脚本在 `/tmp/hf_cache_from_modelscope.py`。跑实验必须带 `HF_HUB_OFFLINE=1`。

## 数据集
`python -m data.prepare_data` 已生成 full(10003) / 5shot(385) / 10shot(770) / 20shot(1540)。

## 基线结果（测试集 Macro-F1，耗时约 1h20m，MPS）

| 方法 | full | 5shot | 10shot | 20shot |
|---|---|---|---|---|
| TF-IDF + LinearSVC | **0.8924** | 0.5126 | 0.6509 | 0.7597 |
| BERT 微调 | 0.8811 | 0.6308 | 0.7997 | **0.8692** |
| SetFit | 0.8707 | **0.7340** | 0.7982 | 0.8369 |

观察：全量数据上 TF-IDF+SVM 反超 BERT 微调 1.1 个点；少样本（5/10-shot）SetFit 明显占优
（5-shot 0.7340 vs BERT 0.6308），20-shot 之后 BERT 追上并反超。

## 进行中
`python -m run_experiments --method all --setting all`（SupCon 消融，3 方法 × 4 设置），
日志 `outputs/ablation.log`，结果增量写入 `outputs/results/ablation_results.json`，可断点续跑。

## 本次补充（傍晚）
- 首次启动 `run_experiments` 忘了设 `HF_HUB_OFFLINE=1`：`from_pretrained` 即使模型已缓存，
  仍对 huggingface.co 发 HEAD 校验，直连超时重试 → 卡住。已 kill 并改用
  `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python -m run_experiments --method all --setting all`
  （日志 `outputs/ablation_run.log`，task mnKJw8），正常跳过已完成项并开始训练 supcon(full)。
- 已把"运行命令速查 + 沙箱/签名坑"补进 MEMORY.md，方便下次直接照抄。
- 待 11 项消融跑完：`python -m analysis.report` 出 `report.md`，并 `python -m analysis.visualize` 补图。
